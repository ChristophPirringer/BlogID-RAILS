$("#food_list").append('<%= j render @food %>');
$("#new_food").remove();
$('#new_food_link').show();
$('#consumed_total').text(<%= current_user.foods.sum(:calories) %>)
$('#calorie_total').text(<%= current_user.foods.sum(:calories) - current_user.exercises.sum(:calories) %>)
